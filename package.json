{
  "name": "Flashlight",
  "version": "0.0.1",
  "description": "Flashlight",
  "main": "app.js",
  "scripts": {
    "setup": "npm install && bower install --config.interactive=false",
    "start": "npm run setup && nodemon app.js -L -i node_modules/ -i public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@bitbucket.org:eugtsukanov/flashlight.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "jsonwebtoken": "^5.4.1",
    "plus.application": "^1.1.2",
    "socket.io": "^1.3.7"
  }
}
